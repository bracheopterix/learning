// we calculate a timer that adds every .1 sec a 5% to a progress bar counter
// we stretch an inner line in a progress bar
// on every 20% there is a heart that glows up red (just change an icon)
// the last heart turns uo in a peach